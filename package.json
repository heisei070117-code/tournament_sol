{
  "name": "tournament-sol",
  "version": "1.0.0",
  "private": true,
  "description": "Browser-only single-elimination tournament bracket creator",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "start": "python3 -m http.server 8080"
  }
}
